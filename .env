# Ports
API_PORT=4000
WEB_PORT=8080
MONGO_PORT=27017

# Server
MONGO_URI=mongodb://mongo:27017/assetdb
JWT_SECRET=change-me-in-prod
# If you want to restrict CORS, set it to your SPA URL; otherwise leave as *
CORS_ORIGIN=http://YOUR-SERVER-IP:8080

# Client build (Option B – nginx proxies /api to server:4000)
VITE_API_BASE=/api
